// These are the default settings used in the browser version

const settings = {
	playSound: true,
	aiSpeed: 'Fast',
	showAnnouncer: true,
	fullScreen: false,
	musicVolume: 80,
	sfxVolume: 60
};

module.exports = {settings};